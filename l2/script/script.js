/*
let userVariable = prompt();
let arrayOfNumbers = [];
function actionsWithVariable(userVariable) {

    let num = 0;


    for (let i = 0; i < 10; i++) {
        switch (userVariable[i]) {
            case '-':
                arrayOfNumbers[num] = i;
                num++;
                break;
            case '+':
                arrayOfNumbers[num] = i;
                num++;
                break;
            case '/':
                arrayOfNumbers[num] = i;
                num++;
                break;
            case '*':
                arrayOfNumbers[num] = i;
                num++;
                break;
            default:
                break;
        }
    }
}
console.log(actionsWithVariable(userVariable));
console.log((userVariable.length - ((arrayOfNumbers[0] * 2) + 1)));
console.log(userVariable.slice((userVariable.length - ((arrayOfNumbers[0] * 2) + 1)), arrayOfNumbers[0]));
console.log(userVariable.slice(arrayOfNumbers[0] + 1));
function ctionsWithVariable1() {
    let sum = 0;
    for (let i = 0; i < arrayOfNumbers.length; i++) {
        switch (userVariable[arrayOfNumbers[i]]) {
            case '+':
                let firstnumber = Number(userVariable.slice((userVariable.length - ((arrayOfNumbers[0] * 2) + 1)), arrayOfNumbers[0]));
                let secondnumber = Number(userVariable.slice(arrayOfNumbers[0] + 1));
                sum = secondnumber + firstnumber;

                break;

            default:
                break;
        }

    }
    console.log(sum);

}
console.log(ctionsWithVariable1())*/



